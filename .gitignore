# macOS smetí
.DS_Store

# Python cache a virtuální prostředí
__pycache__/
*.py[cod]
.venv/
venv/

# Pracovní podadresář skillu (venv, cache, stažená data, configy, PNG náhledy)
work/

# Vygenerované výstupy (PDF grafy a plány + PNG náhledy stran a stažená data)
*.pdf
*_page[0-9]*.png
*.json

# Lokální konfigurace Claude Code (necommitujeme uživatelská nastavení)
.claude/settings.local.json
